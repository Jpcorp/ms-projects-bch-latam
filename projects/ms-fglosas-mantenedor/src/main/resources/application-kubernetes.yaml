######
# Configuracion Kubernetes Config
######

spring:
  config:
    import: "kubernetes:"
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - name: ms-fglosas-mantenedor
          - name: fglosas-bbdd
          - name: fglosas-rest
          

